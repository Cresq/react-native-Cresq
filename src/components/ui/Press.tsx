/**
 * The name the press went by before the motion system had one. Every control
 * that imports `Press` gets `AnimatedPressable`; new code should ask for that.
 */
export { AnimatedPressable as Press } from "./AnimatedPressable";
